library(sf)
library(bcdata) # Requires PR #https://github.com/bcgov/bcdata/pull/250
library(bcmaps)

# bc_bound_hres <- bc_bound_hres()
# options("bcdata.chunk-limit" = 100)
# bec <- bcdc_get_data(bcdc_search("BEC Map")[[1]])
# saveRDS(bec, here::here("./data-raw/bec.RDS"))
# bec <- readRDS(here::here("./data-raw/bec.RDS"))

library(leaflet)
library(mapboxapi)





